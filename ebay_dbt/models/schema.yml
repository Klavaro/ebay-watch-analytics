version: 2

sources:
  - name: ebay_raw_source
    database: EBAY_ANALYTICS
    schema: RAW
    tables:
      - name: EBAY_RAW
        columns:
          - name: ITEM_ID
            tests:
              - not_null

snapshots:
  - name: snap_seller
    description: "Snapshot for tracking changes to seller attributes (Type 2 SCD)"
    columns:
      - name: seller_username
        description: "Natural key for identifying the seller"
        tests:
          - not_null
          - unique
      - name: seller_feedback_score
        description: "Seller's feedback score"
      - name: seller_feedback_percentage
        description: "Seller's positive feedback percentage"
      - name: load_timestamp
        description: "When the record was extracted from the source"

models:
  - name: stg_ebay_items
    description: "Flattened source of eBay item listings"
    columns:
      - name: item_id
        tests: [not_null, unique]
      - name: title
      - name: condition
      - name: condition_id
      - name: category_id
      - name: category_name
      - name: seller_username
      - name: seller_username_cleaned
      - name: seller_feedback_percentage
      - name: seller_feedback_score
      - name: listing_marketplace_id
      - name: item_location_country
      - name: price_value
      - name: price_currency
      - name: adult_only
      - name: available_coupons
      - name: top_rated_buying_experience
      - name: priority_listing
      - name: item_origin_date
      - name: item_creation_date
      - name: load_timestamp

  - name: dim_condition
    description: "Condition dimension with surrogate key"
    columns:
      - name: condition_sk
        tests: [not_null, unique]
      - name: condition_id
      - name: condition

  - name: dim_category
    description: "Leaf category dimension"
    columns:
      - name: category_sk
        tests: [not_null, unique]
      - name: category_id
      - name: category_name

  - name: dim_seller
    description: "Seller dimension with surrogate key"
    columns:
      - name: seller_sk
        tests: [not_null, unique]
      - name: seller_username
      - name: seller_feedback_score
      - name: seller_feedback_percentage

  - name: dim_date
    description: "Date dimension with surrogate key"
    columns:
      - name: date_sk
        tests: [not_null, unique]
      - name: date
      - name: year
      - name: month
      - name: day
      - name: quarter
      - name: day_of_week

  - name: fact_item_listing
    description: "Fact table with one row per item listing"
    columns:
      - name: item_listing_sk
        tests: [not_null, unique]
      - name: item_id
        tests: [not_null]
      - name: title
      - name: condition_sk
        tests:
          - relationships:
              to: ref('dim_condition')
              field: condition_sk
      - name: category_sk
        tests:
          - relationships:
              to: ref('dim_category')
              field: category_sk
      - name: seller_sk
        tests:
          - relationships:
              to: ref('dim_seller')
              field: seller_sk
      - name: origin_date_sk
        tests:
          - relationships:
              to: ref('dim_date')
              field: date_sk
      - name: creation_date_sk
        tests:
          - relationships:
              to: ref('dim_date')
              field: date_sk
      - name: load_date_sk
        tests:
          - relationships:
              to: ref('dim_date')
              field: date_sk
      - name: marketplace_id
      - name: item_location_country
      - name: price_value
      - name: price_currency
      - name: adult_only
      - name: available_coupons
      - name: top_rated_buying_experience
      - name: priority_listing

  - name: bridge_item_buying_option
    description: "Bridge table mapping listings to buying options"
    columns:
      - name: item_id
        tests:
          - relationships:
              to: ref('fact_item_listing')
              field: item_id
      - name: buying_option

  - name: bridge_item_shipping_option
    description: "Bridge table with shipping cost and region info"
    columns:
      - name: item_id
        tests:
          - relationships:
              to: ref('fact_item_listing')
              field: item_id
      - name: shipping_cost_type
      - name: shipping_cost
      - name: shipping_currency
      - name: ship_to_locations

  - name: bridge_item_image
    description: "Bridge table mapping item listings to image URLs"
    columns:
      - name: item_id
        tests:
          - relationships:
              to: ref('fact_item_listing')
              field: item_id
      - name: image_url
      - name: image_type
  - name: mart_category_price_bands
    description: "Provides price distribution metrics per category for identifying investment opportunities"
    columns:
      - name: category_sk
        description: "Surrogate key for category"
        tests:
          - not_null
      - name: price_p25
        description: "25th percentile of price values"
      - name: price_median
        description: "50th percentile (median) of price values"
      - name: price_p75
        description: "75th percentile of price values"
      - name: avg_price
        description: "Average price in category"
      - name: price_stddev
        description: "Standard deviation of price in category"

  - name: mart_item_performance
    description: >
      Aggregated performance metrics for each item listing including price, coupon status,
      and priority listing flag to support business insights into listing effectiveness and pricing strategy.
    columns:
      - name: item_listing_sk
        description: Surrogate key for the item listing.
        tests:
          - not_null
          - unique
      - name: item_id
        description: Original item ID from the eBay dataset.
        tests:
          - not_null
          - unique
      - name: price_value
        description: Price of the item at the time of data capture.
        tests:
          - not_null
          - greater_than:
              value: 0
      - name: price_currency
        description: Currency used in pricing.
        tests:
          - not_null
      - name: available_coupons
        description: Boolean flag indicating if coupons were available.
        tests:
          - accepted_values:
              values: [true, false]
      - name: priority_listing
        description: >
          Boolean flag indicating whether the item is considered high-priority,
          which can affect its visibility and click-through rate.
        tests:
          - accepted_values:
              values: [true, false]
      - name: top_rated_buying_experience
        description: Flag indicating if the item provides a top-rated buying experience.
        tests:
          - accepted_values:
              values: [true, false]
      - name: item_location_country
        description: Country where the item is shipped from.
        tests:
          - not_null
      - name: creation_date_sk
        description: Foreign key to the date dimension when the item was created.
        tests:
          - not_null
          - relationships:
              to: ref('dim_date')
              field: date_sk
      - name: origin_date_sk
        description: Foreign key to the date dimension for the item's origin date.
        tests:
          - not_null
          - relationships:
              to: ref('dim_date')
              field: date_sk

  - name: mart_condition_price_index
    description: "Analyzes average and indexed price values by item condition"
    columns:
      - name: condition_sk
        description: "Surrogate key for condition"
        tests:
          - not_null
      - name: item_count
        description: "Number of items listed with this condition"
      - name: avg_price
        description: "Average price of items in this condition"
      - name: min_price
        description: "Minimum price"
      - name: max_price
        description: "Maximum price"
      - name: normalized_price_score
        description: "Standardized price score compared to other conditions"
